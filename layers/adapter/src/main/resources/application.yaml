management:
  endpoint:
    health:
      probes:
        enabled: true
      show-details: always

spring:
  application:
    name: hexagonal-poc
  datasource:
    url: jdbc:postgresql://localhost:5433/hexagonal-poc
    username: hexagonal-poc
    password: hexagonal-poc
  jpa:
    generate-ddl: true
    show-sql: true

springdoc:
  show-actuator: true